// Schema moved to prisma/schema/ folder (multi-file approach)
// base.prisma    → generator + datasource
// enum.prisma    → all enums
// auth.prisma    → User, Profile, RefreshToken, Wishlist, Cart, CartItem
// product.prisma → Category, SubCategory, Product
// listing.prisma → SellerAd, AdImage, InterestedUser
// admin.prisma   → Report, AdminLog
// logs.prisma    → ActivityLog, Notification
